<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.SprawdzianWiktorKycia4D" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="Theme.SprawdzianWiktorKycia4D" parent="Base.Theme.SprawdzianWiktorKycia4D" />

    <style name="heading">
        <item name="android:textColor">@color/primaryColor</item>">
        <item name="android:textSize">32sp</item>
        <item name="android:textAlignment">center</item>
        <item name="android:layout_marginBottom">30dp</item>
    </style>

    <style name="label">
        <item name="android:textColor">@color/labelColor</item>
        <item name="android:textSize">18sp</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:layout_marginBottom">15dp</item>
    </style>

    <style name="reported" parent="label">
        <item name="android:textColor">@color/primaryColor</item>
        <item name="android:layout_marginBottom">20dp</item>
    </style>

    <style name="input">
        <item name="android:textSize">16sp</item>
        <item name="android:textAlignment">viewStart</item>
        <item name="android:layout_marginBottom">10dp</item>
        <item name="android:textColor">@color/primaryColor</item>
    </style>

    <style name="button">
        <item name="android:backgroundTint">@color/primaryColor</item>
        <item name="android:textColor">@color/white</item>
    </style>

</resources>